{
    "require": {
        "illuminate/database": "*"
    },
    "autoload": {
        "classmap": [
        	"controllers",
        	"core",
        	"helpers",
        	"models"
        ]
    }
}